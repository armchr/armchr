/**
 * Global feature flags for the Armchair frontend.
 * Toggle features on/off without removing code.
 */

// Code Review feature (Reviewer Agent): review buttons on commits,
// uncommitted changes, PRs, the Reviews tab, and review results display.
export const FEATURE_REVIEW_ENABLED = false;
