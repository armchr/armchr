# Backend Configuration
# Copy this file to .env and update values as needed

# Server port
PORT=8787

# Output directory for split patches and reviews
ARMCHAIR_OUTPUT=../../output

# Root directory constraint (all repository paths must be under this)
ARMCHAIR_ROOT_DIR=$HOME

# LLM Configuration (required for AI features)
ARMCHAIR_MODEL_API_KEY=your-api-key-here
ARMCHAIR_MODEL_API_BASE_URL=https://api.openai.com/v1
ARMCHAIR_MODEL_NAME=gpt-4

# External Tool Paths (defaults shown)
CODE_REVIEWER_PATH=../../code_reviewer/code-reviewer
CODE_REVIEWER_APP_CONFIG=../../code_reviewer/configs/app.yaml
SPLITTER_PATH=../../splitter_dep
# PYTHON_PATH=../../splitter_dep/venv/bin/python3  # Auto-detected if not set

# Cache Configuration
CACHE_REFRESH_INTERVAL_MS=1800000  # 30 minutes

# Development Mode (preserves temp files for debugging)
DEV_MODE=false
